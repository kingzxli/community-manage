server:
  port: 8081
  
spring:
  # 环境 dev开发时用环境 | test测试环境 | pro上线时用环境
  profiles:
    active: dev
  main:
    allow-bean-definition-overriding: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
    
## 切换环境：java -jar xxx.jar --spring.profiles.active=dev